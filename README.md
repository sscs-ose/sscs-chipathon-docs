# sscs-chipathon-docs

## Contribution from a Collaborator

- `git clone git@github.com:sscs-ose/sscs-chipathon-docs.git`
- Make contribution
- `git commit -m "comment" -a`
- `git push 
  - add the apprpriate ssh identity (eg. silicon-vlsi) before pushing
